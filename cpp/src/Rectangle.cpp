
class Rectangle {

	bool is_full = false;
	int x, y;
	int width = 60, height = 60;

	void init(int x, int y) {
		this->is_full = is_full;
		this->x = x * width + 5; 
		this->y = y * height + 5;

		# we have to make the square show the square sprite at x y

		return;
	};



};
